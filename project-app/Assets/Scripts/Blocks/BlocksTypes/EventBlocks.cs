using System.Collections.Generic;


public static class EventBlocks
{
    // Lista estática de bloques de eventos
    public static readonly List<(string text, string iconPath)> Blocks = new List<(string text, string iconPath)>
    {
        ("al hacer clic en", ""),
    
    };
}
